import { ChangeTextoDirective } from './change-texto.directive';

describe('ChangeTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeTextoDirective();
    expect(directive).toBeTruthy();
  });
});
